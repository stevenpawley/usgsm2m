% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m2m-download.R
\name{M2MDownloadOptions}
\alias{M2MDownloadOptions}
\title{Products available for download}
\description{
Returned by `M2MSceneSearch$products()`. Holds what the M2M API will let
you download for a set of scenes, and lets you narrow it before placing a
request. Not created directly.

The API offers downloads at two granularities, and this object exposes
both:

* **Individual files** - the bands and per-file products nested inside a
  product's `secondaryDownloads`, listed by `$bands()`. This is the
  default selection.
* **Whole products** - the scene-level entries the API lists, such as a
  Level-1 Product Bundle (a single `.tar` for the scene) or a
  full-resolution browse image, listed by `$scene_products()`. Some of
  these have no constituent files at all and so never appear in
  `$bands()`.

Both are requestable. Use `$select_bands()` to queue individual files, or
`$select_products()` to queue whole products; `$selected()` always shows
what `$request()` will actually queue.
}
\section{Public fields}{
  \if{html}{\out{<div class="r6-fields">}}
  \describe{
    \item{\code{products}}{A scene-level tibble of products, with the individual
bands nested in a `secondaryDownloads` list-column.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-M2MDownloadOptions-initialize}{\code{M2MDownloadOptions$new()}}
    \item \href{#method-M2MDownloadOptions-selected}{\code{M2MDownloadOptions$selected()}}
    \item \href{#method-M2MDownloadOptions-bands}{\code{M2MDownloadOptions$bands()}}
    \item \href{#method-M2MDownloadOptions-scene_products}{\code{M2MDownloadOptions$scene_products()}}
    \item \href{#method-M2MDownloadOptions-select_bands}{\code{M2MDownloadOptions$select_bands()}}
    \item \href{#method-M2MDownloadOptions-select_products}{\code{M2MDownloadOptions$select_products()}}
    \item \href{#method-M2MDownloadOptions-filter}{\code{M2MDownloadOptions$filter()}}
    \item \href{#method-M2MDownloadOptions-request}{\code{M2MDownloadOptions$request()}}
    \item \href{#method-M2MDownloadOptions-print}{\code{M2MDownloadOptions$print()}}
    \item \href{#method-M2MDownloadOptions-clone}{\code{M2MDownloadOptions$clone()}}
  }
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-initialize}{}}}
\subsection{\code{M2MDownloadOptions$new()}}{
  Wrap download options. Use `M2MSceneSearch$products()`
  instead.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$new(session, products, scene_list, selected = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{session}}{The parent [M2MSession].}
      \item{\code{products}}{A tibble of products from the download-options endpoint.}
      \item{\code{scene_list}}{The [M2MSceneList] the products belong to.}
      \item{\code{selected}}{An optional pre-narrowed band tibble.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-selected"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-selected}{}}}
\subsection{\code{M2MDownloadOptions$selected()}}{
  The rows `$request()` will queue. Individual files by
  default, or whole products after `$select_products()`.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$selected()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble of downloadable items.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-bands"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-bands}{}}}
\subsection{\code{M2MDownloadOptions$bands()}}{
  The currently selected downloads. An alias for
  `$selected()`, kept because the selection is individual bands in the
  common case.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$bands()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble of downloadable items.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-scene_products"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-scene_products}{}}}
\subsection{\code{M2MDownloadOptions$scene_products()}}{
  The whole-product entries the API lists for these scenes,
  one row each, such as a Level-1 Product Bundle or a full-resolution
  browse image.

  These are requestable in their own right and are a different unit
  from `$bands()`: a bundle is a single `.tar` holding the whole scene,
  where `$bands()` would list its contents as separate files. Products
  with no constituent files, such as browse imagery, appear only here.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$scene_products()}
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A tibble of products, without the `secondaryDownloads` column.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-select_bands"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-select_bands}{}}}
\subsection{\code{M2MDownloadOptions$select_bands()}}{
  Select the individual files whose `displayId` matches any
  of the given patterns.

  Patterns are matched against `displayId`, whose values you can see in
  `$bands()`. Matching nothing warns and lists what was available.

  Like `$select_products()`, this selects afresh from everything
  available at that granularity rather than narrowing an existing
  selection - use `$filter()` to narrow. It also does not filter on
  availability; chain `$filter(bulkAvailable)` if you want only
  bulk-downloadable files.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$select_bands(patterns)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{patterns}}{A character vector of patterns, e.g. `c("B4", "B5")`.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new [M2MDownloadOptions] with those files selected.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-select_products"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-select_products}{}}}
\subsection{\code{M2MDownloadOptions$select_products()}}{
  Switch the selection to whole products rather than the
  individual files inside them, optionally keeping only those whose
  `productName` matches one of the given patterns.

  Patterns are matched against `productName`, whose values come from
  `$scene_products()`. They differ between datasets - the bundle is
  "Landsat Collection 2 Level-2 Product Bundle" for `landsat_ot_c2_l2`
  but "Standard Format" for `corona2` - so check `$scene_products()`
  rather than reusing a pattern from another dataset. Matching nothing
  warns and lists what was available.

  As with `$select_bands()` this does not filter on availability;
  chain `$filter(available)` to drop products the API has marked
  unavailable.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$select_products(patterns = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{patterns}}{An optional character vector of patterns to match
against `productName`, e.g. `"Product Bundle"`.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new [M2MDownloadOptions] with the products selected.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-filter"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-filter}{}}}
\subsection{\code{M2MDownloadOptions$filter()}}{
  Narrow the current selection using `dplyr::filter()`
  semantics against the tibble returned by `$selected()`.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$filter(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Expressions passed to `dplyr::filter()`.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    A new [M2MDownloadOptions] with the narrowed selection.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-request"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-request}{}}}
\subsection{\code{M2MDownloadOptions$request()}}{
  Place a download request for the current selection. Files
  the distribution system can serve immediately come back with URLs;
  anything needing preparation is collected later via
  `M2MDownloadQueue$refresh()`.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$request(label)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{label}}{A label identifying this order in the download queue.
Required, and used to find the order again later.}
    }
    \if{html}{\out{</div>}}
  }
  \subsection{Returns}{
    An [M2MDownloadQueue] object.
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-print"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-print}{}}}
\subsection{\code{M2MDownloadOptions$print()}}{
  Print a summary of the available and selected downloads.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$print(...)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{...}}{Ignored.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-M2MDownloadOptions-clone"></a>}}
\if{latex}{\out{\hypertarget{method-M2MDownloadOptions-clone}{}}}
\subsection{\code{M2MDownloadOptions$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{M2MDownloadOptions$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
